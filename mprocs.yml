# procs:
#   inngest:
#     cmd: ["npm", "run", "inngest:dev"]

#   next:
#     cmd: ["npm", "run", "dev"]

procs:
  next:
    cmd: sh -c "npm run dev"

  inngest:
    cmd: sh -c "npm run inngest:dev"
